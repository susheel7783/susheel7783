// stack reverse by using two stack
#include <iostream>
#include<stack>
using namespace std;
int main(){
	stack<int>s1;
	s1.push(1);
	s1.push(2);
	s1.push(3);

	stack<int>st;
	while(!s1.empty()){
	st.push(s1.top());
	s1.pop();
}
	while(!st.empty()){
		cout<<st.top()<<" ";
		st.pop();
	}

}
